"""Application layer: orchestration services that compose core + infrastructure.

Contains the OCR pipeline, queue manager, parallel processor, profile manager,
export manager and the OCRmyPDF integration wrapper. No UI or Qt imports.
"""
